import 'package:flutter/material.dart';

class Pricing_Screen extends StatelessWidget {
  const Pricing_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}